coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: 'condensed_header, diff, flags, files'
  require_changes: true

flag_management:
  individual_flags:
    - name: tests
      paths:
        - src/comunio_mcp/
